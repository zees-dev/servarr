{{ .Release.Name }} chart {{ .Chart.Version }} in namespace {{ .Release.Namespace }}.

Check rollout and setup jobs:
  kubectl -n {{ .Release.Namespace }} get pods,jobs
  helm status {{ .Release.Name }} -n {{ .Release.Namespace }}

{{ if .Values.qbittorrent.addons.gluetun.enabled -}}
VPN enabled: verify a provider exit address and failed clear-interface connections
before adding torrents. The cluster must enforce NetworkPolicy.
{{ else -}}
qBittorrent uses direct networking. Supply the documented VPN values and Secret
if you require a VPN.
{{ end }}
See the chart README for setup behavior, retained claims and upgrade notes.
